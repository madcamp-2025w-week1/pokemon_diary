import 'package:flutter/material.dart';

class Tab3Pokedex extends StatelessWidget {
  const Tab3Pokedex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pokedex Tab'),
    );
  }
}
