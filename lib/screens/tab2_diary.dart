import 'package:flutter/material.dart';

class Tab2Diary extends StatelessWidget {
  const Tab2Diary({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Diary Tab'),
    );
  }
}
